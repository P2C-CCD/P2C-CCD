# Website Images

This directory stores web-facing image assets for the GitHub Pages project page.

## Subfolders

- `paper/`: release-local copies of the figures referenced by the project page.

The website should reference these copied assets rather than external working
directories, so the page stays self-contained after deployment.
